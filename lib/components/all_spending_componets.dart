import 'package:flutter/material.dart';

class AllSpendingComponets extends StatelessWidget {
  const AllSpendingComponets({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("All Spending"),
    );
  }
}
